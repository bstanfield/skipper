script.
    var $ = require('jquery');
    $(window).on("scroll", function() {
    var scrollPos = $(window).scrollTop();
        if (scrollPos <= 15) {
            $('.navbar').removeClass('top');
        } else {
            $('.navbar').addClass('top');
        }
    });
    
.navbar
    .nav-items
        .nav-burger
            span.open(id="open" onclick="openNav()") &#9776
        a.nav-logo(href='/')
            img.medium(src='images/skipper-logo.png')
        .nav-menu
            //- a.nav-link(href="index.html") Home
            a.nav-link(href='about.html') About
            a.nav-link(href='blog.html') Blog
            a.nav-link.demo-link(href='request-demo.html?email=') Request Demo
            
            form#demoForm(action="/request-demo.html?email=" method="GET")
                input.input-box.input-nav(type="email" placeholder="Enter email" name="email")
                input#email_submit.button.button-nav(type="submit" value="Request Demo")

    //- script.
    //-     var $ = require('jquery');
    //-     $('#demoForm').submit(function(a){
    //-         a.preventDefault();
    //-         $.ajax({
    //-             url:'',
    //-             type:'post',
    //-             data:$('#demoForm').serialize(),
    //-             success:function(){
    //-                 window.location = "/success.html";
    //-             }
    //-         });
    //-     });